<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.EntropyCalc" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/bg_light_layer</item>
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="Theme.EntropyCalc" parent="Base.Theme.EntropyCalc" />
</resources>